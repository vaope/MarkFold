PRD：个人碎片信息管理助手（精简版 V1.1）
1. 产品概述
1.1 产品名称

个人碎片信息管理助手

1.2 产品定位

一个基于 LLM 的个人工作记忆助手。用户可通过 Web、CLI、移动端发送文字和图片，系统自动识别所属工作项、提炼结构化信息，并同步更新对应的 Markdown 文档；同时支持导入已有文档，或通过对话命令快速初始化新工作项。

1.3 产品目标

降低用户维护工作项文档和 Todo 的成本，让“想到就发”成为默认工作流，同时保留文档化沉淀能力。

2. 核心目标
2.1 MVP 核心闭环

用户输入内容
→ 系统识别/定位工作项
→ LLM 提炼结构化信息
→ 更新数据层
→ 同步更新 Markdown 工作项文档
→ 用户可继续通过对话查询、修正、追问

2.2 本期重点

支持已有文档导入

支持对话命令初始化工作项

支持 Markdown 作为唯一文档格式

支持多端输入接入

支持图片作为附件写入文档

支持后续扩展到 Web / CLI / 飞书移动端

3. 产品范围
3.1 本期范围

文本输入

图片附件输入

导入已有 Markdown 文档

通过特殊标识初始化工作项文档

工作项自动识别

信息结构化

Todo 自动创建/完成更新

Markdown 文档自动同步

工作项查询

昨日回顾 / Todo 查询

多端统一能力层设计

3.2 非本期范围

OCR / 图片内容识别

多人协作编辑冲突解决

企业级权限体系

富文本/Notion/Docs 原生格式写回

复杂提醒和日历深度联动

4. 用户与使用场景
4.1 目标用户

同时跟进多个事项的知识工作者

有文档沉淀习惯的人

希望通过聊天方式管理工作项的人

4.2 核心场景
场景 A：碎片更新

用户发一句自然语言，系统自动更新某工作项。

场景 B：已有文档接入

用户把自己原本维护的 Markdown 文档导入系统，系统将其注册为一个工作项。

场景 C：对话初始化

用户在对话中通过特殊命令快速新建一个工作项文档。

场景 D：多端输入

用户可能在：

Web 上集中查看与编辑

CLI 中快速记录

飞书 App 中随手发送消息/图片

场景 E：回顾与追问

用户查询：

我昨天干了什么

我还有哪些 Todo

这个工作项做到哪了

5. 核心概念
5.1 工作项 Work Item

持续跟进的事项单元，对应一个 Markdown 文档。

5.2 输入记录 Input Entry

一次用户输入，可能来自 Web、CLI、飞书。

5.3 结构化事件 Structured Event

从输入中提炼出的标准信息单元：

progress

todo_created

todo_completed

risk

decision

note

5.4 工作项文档

每个工作项对应一个 Markdown 文档，是最终存档载体。

5.5 渠道 Channel

输入来源端：

web

cli

feishu_mobile

6. 功能需求
6.1 多端接入模块
功能描述

系统需支持多种输入入口，但共用统一的识别、归档、文档更新能力。

支持渠道

Web 端

CLI 端

移动端（当前优先通过飞书 App 通信）

设计原则

渠道层只负责收发消息

核心业务逻辑在服务端统一处理

不同端使用统一的工作项、事件、文档模型

说明

飞书在本项目中优先作为消息入口，而不是独立业务系统。移动端体验先以“通过飞书给助手发消息”为主。

6.2 输入模块
功能描述

支持用户输入文字、图片、图文混合内容。

要求

每条输入带时间戳、渠道来源、用户标识

原始输入必须保留

图片仅作为附件处理，不解析图片内容

输入与后续结构化结果必须可追溯

6.3 工作项初始化模块
功能描述

支持两种初始化工作项的方式：

导入已有文档

通过对话特殊标识创建新工作项

6.3.1 导入已有文档
功能描述

用户可上传或指定自己已编辑好的 Markdown 文档，将其注册为一个工作项。

支持能力

导入 .md 文件

解析文档基础结构

自动生成工作项记录

建立文档路径与工作项绑定

可选提取标题、状态、Todo 初始内容

导入后结果

系统生成：

WorkItem

文档路径绑定

初始索引

可供后续继续追加更新

原则

导入后尽量不重写原文，只在后续新增内容时按模板区块追加。

6.3.2 对话初始化工作项
功能描述

用户可通过对话中的特殊标识快速创建一个新的工作项及对应 Markdown 文档。

示例
/init 项目A

或

#new 项目A

或更完整形式：

/init
title: 项目A
goal: 完成 MVP 设计
status: 进行中
系统行为

创建新的 WorkItem

生成对应 Markdown 文档

写入初始模板

返回创建结果

要求

特殊标识语法需简单、稳定、可跨端复用，CLI/Web/飞书应尽量统一。

6.4 工作项识别模块
功能描述

自动判断当前输入归属于哪个工作项。

识别信号

标题/别名关键词

历史语义相似度

最近会话上下文

Todo 关联

最近活跃工作项

渠道会话上下文（例如飞书某连续会话）

策略

高置信度：直接写入

中置信度：写入并提示可修正

低置信度：进入待确认

6.5 信息结构化模块
功能描述

使用 LLM 对输入内容进行结构化提炼。

输出类型

进展更新

新 Todo

Todo 完成

风险/阻塞

决策

普通备注

要求

一次输入支持拆成多个事件

输出应标准化，便于后续写入 Markdown

支持后续扩展新的事件类型

6.6 Todo 管理模块
功能描述

自动维护每个工作项的待办状态。

支持能力

新 Todo 提取

Todo 完成识别

Todo 状态更新

查询未完成 Todo

按工作项聚合 Todo

扩展性要求

Todo 模型后续应支持：

priority

due_date

assignee（即使当前是个人版，也先预留）

source_channel

6.7 图片附件归档与文档插入模块
功能描述

支持将用户发送的图片作为附件归档，并插入对应 Markdown 文档。

本期约束

不做 OCR

不做图片语义识别

只处理图片存储、路径引用、文档插入

插入方式

图文混合输入：文字与图片作为同一条记录写入

纯图片输入：以附件记录形式写入

多图输入：按顺序插入

6.8 Markdown 文档同步模块
功能描述

系统在归档成功后自动更新对应工作项的 Markdown 文档。

文档作用

作为可见存档

作为用户长期维护的主载体

作为跨端统一落地格式

更新原则

尽量追加，不覆盖旧内容

使用稳定模板区块

Todo 使用 checklist

图片使用 Markdown 引用

更新失败时不得丢失结构化结果

6.9 查询与汇总模块
功能描述

支持按工作项和按时间范围查询。

查询示例

我昨天干了什么

我还有哪些 Todo

项目 A 最近进展如何

这个事项现在卡在哪

输出原则

按工作项聚合优先

优先展示完成项、进展、阻塞、Todo

不直接堆叠原始输入

6.10 待确认与纠错模块
功能描述

处理归档不确定、工作项识别错误、事件分类错误等问题。

支持能力

改绑工作项

修改事件类型

撤销写入

重写文档区块

7. Markdown 文档规范
7.1 基础模板
# 工作项标题

## 基本信息
- 状态：
- 创建时间：
- 最近更新时间：
- 来源：import / init / auto_created

## 背景
...

## 当前目标
...

## 最新进展
- YYYY-MM-DD：...

## 当前 Todo
- [ ] ...
- [ ] ...

## 已完成事项
- [x] ...

## 风险 / 阻塞
- ...

## 决策 / 备注
- ...

## 附件记录
### YYYY-MM-DD
- 说明：...
- 图片：![](./attachments/xxx.png)
7.2 导入文档兼容原则

若用户导入的 Markdown 不完全符合模板：

保留原文

系统补充必要元信息

后续新增内容优先写入标准区块

若缺失区块，则自动补建

8. 特殊标识设计
8.1 目标

让用户在对话中快速完成显式控制，而不依赖 UI。

8.2 建议首批命令
/init <title>

创建新工作项

/use <title>

显式切换当前工作项上下文

/todo <content>

显式新增 Todo

/done <content>

显式完成 Todo

/status <title>

查询工作项状态

8.3 原则

语法统一

可跨 Web / CLI / 飞书复用

后续便于增加更多命令

9. 架构与扩展性要求
9.1 总体架构原则

采用渠道层 + 核心服务层 + 存储层 + 文档层分层架构，避免端能力和业务逻辑耦合。

A. 渠道层

负责接入：

Web

CLI

飞书 Bot / 飞书消息网关

B. 核心服务层

负责：

工作项识别

LLM 结构化

Todo 更新

查询汇总

待确认处理

C. 存储层

负责：

WorkItem

InputEntry

StructuredEvent

Todo

Attachment

D. 文档层

负责：

Markdown 文档生成

Markdown 文档增量更新

附件路径管理

9.2 扩展性要求
渠道扩展

新增渠道时，不应修改核心归档逻辑。
例如未来增加：

微信机器人

Telegram

邮件转发入口

文档扩展

虽然当前只支持 Markdown，但应预留文档抽象层，未来可扩展：

Notion

飞书文档

Google Docs

LLM 扩展

应将 LLM 能力抽象为标准接口，支持替换不同模型或增加多模型策略。

存储扩展

附件、文档、结构化数据建议分离存储，方便后期迁移和扩容。

10. 多端形态建议
10.1 Web 端
定位

主工作台，适合：

查看工作项列表

查看文档

修正归档

查询汇总

导入文档

10.2 CLI 端
定位

快速输入和脚本化使用入口，适合：

快速记录

通过命令创建工作项

查询 Todo

本地开发者使用

10.3 移动端（飞书）
定位

移动侧输入入口，适合：

随手发消息

发图片附件

快速追问

查看简版回复

当前建议

第一阶段将飞书视为“消息壳”，核心逻辑仍在服务端。
移动端暂不单独开发 App。

11. 数据对象建议
WorkItem

id

title

aliases

status

source_type（import/init/auto）

markdown_doc_path

created_at

updated_at

InputEntry

id

channel

raw_text

attachment_ids

created_at

linked_work_item_id

confidence

review_status

StructuredEvent

id

work_item_id

type

content

source_input_id

created_at

Todo

id

work_item_id

content

status

priority（预留）

due_date（预留）

source_event_id

created_at

completed_at

Attachment

id

file_path

file_type

source_input_id

linked_work_item_id

created_at

12. 非功能要求
可追溯性

所有文档更新都必须可追溯到原始输入。

可恢复性

文档写入失败时，事件和附件不能丢失。

一致性

结构化数据与 Markdown 文档尽量保持同步一致。

可扩展性

新增渠道、命令、文档类型时，不应推翻现有架构。

13. 版本规划
V1

Markdown 文档

导入已有 Markdown

对话命令初始化工作项

Web / CLI / 飞书消息接入

自动归档

Todo 管理

图片附件插入

昨日回顾 / Todo 查询

V2

更完整的工作台

更强的待确认机制

文档差异回滚

更丰富命令体系

周报/月报

V3

多文档后端支持

多模型策略

更复杂的提醒与优先级建议

多人协作预留

14. MVP 验收标准

满足以下条件即可视为本期可用：

用户可导入一个已有 Markdown 文档并注册为工作项

用户可通过 /init 创建一个新工作项文档

用户通过 Web、CLI、飞书任一入口发消息，系统都能统一归档

用户输入文字后，系统能更新对应 Markdown 文档

用户发送图片后，系统能将图片作为附件插入文档

用户可查询“我昨天干了什么”

用户可查询“我还有哪些 Todo”

用户可修正归档错误